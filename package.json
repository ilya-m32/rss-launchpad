{
  "name": "rss-lauchpad",
  "version": "0.2.0",
  "description": "RSS Launchpad extension: quickly add new subscriptions from websites",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc && ./build.bash",
    "build:ext": "web-ext build -s dist_ext",
    "start": "npm run build && web-ext run --devtools -u \"https://en.wikipedia.org/wiki/Main_Page\"",
    "lint:ext": "web-ext lint",
    "lint:src": "prettier . --check",
    "lint:fix": "prettier . --write",
    "lint": "npm run lint:src && npm run lint:ext"
  },
  "keywords": [
    "extension",
    "browser",
    "rss"
  ],
  "author": "Ilya Malyavin",
  "license": "MIT",
  "dependencies": [],
  "devDependencies": {
    "@types/chrome": "^0.0.299",
    "@types/firefox-webext-browser": "^120.0.4",
    "@types/node": "^22.10.7",
    "prettier": "3.4.2",
    "typescript": "^5.7.3",
    "web-ext": "^8.4.0"
  }
}
